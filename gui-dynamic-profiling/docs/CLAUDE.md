# CLAUDE.md — docs/

Project documentation beyond the root README.

## Files

### `components.md` — Component Reference

Detailed reference for every Python module in the project. For each module, documents:

- Purpose and responsibilities
- Public function signatures (table format)
- Dash component IDs managed
- Callback input/output wiring

Covers: `app.py`, `data_loader.py`, all `layouts/` modules, and all `callbacks/` modules.

## Protocol

Every modification on this CLAUDE.md should be reflected (i.e., updated) in the CLAUDE.md in its parent directory, recursively if applicable.
